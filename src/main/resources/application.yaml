spring:
  application:
    name: database
  datasource:
    url: jdbc:postgresql://postgres-db:5432/appdb
    username: user
    password: password

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

server:
  port: 8081
  address: 0.0.0.0
