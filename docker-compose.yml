version: "3.9"

services:
  app:
    build: .
    container_name: java-app
    restart: always
    ports:
      - "8081:8081"
    networks:
      - app-network

networks:
  app-network:
    external: true